- defaults:
    link:
        create: true
        relink: true

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.bashrc.d/global.bash: bash/global.bash
    ~/.vimrc:

- create:
    - ~/.vim/backup
    - ~/.vim/swap
    - ~/.vim/undo

- shell:
    - [touch ~/.vimrc.local]
