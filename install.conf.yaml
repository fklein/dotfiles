- defaults:
    link:
        create: true
        relink: true

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.vimrc:

- create:
    - ~/.vim/backup
    - ~/.vim/swap
    - ~/.vim/undo

- shell:
    - [touch ~/.vimrc.local]
